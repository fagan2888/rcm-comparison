% Finding all path from Original -> Destination.
% Breadth-first search
% ---------------------------------------------

function paths = allPathsOD(orgi, dest, Incidence, maxNpaths)
    
end

function [] = findPath(path, node, isVisited)
    global Incidence;
    
end